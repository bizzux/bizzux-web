node_modules
.next
.env
.env.local
.vercel
*.log
scripts/migrate.env

# Files Claude moves here instead of deleting outright (it can't delete on
# your machine) when cleaning up old code — safe to permanently delete this
# folder any time; nothing in the app references it.
_to_delete

.vercel
.env*
